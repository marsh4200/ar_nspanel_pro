{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ar-nspanel-pro.example/schema/panels.schema.json",
  "title": "AR NSPanel Pro config (panels JSON)",
  "description": "The single config document consumed by BOTH sides (PLAN.md §3.3 + §3.6). The app renders pages/tiles and ignores `bindings`; the HA integration executes `bindings` and ignores visual props. Kept intentionally permissive on visual-only fields (the app tolerates unknowns) but strict on structure the integration relies on.",
  "type": "object",
  "required": [
    "v",
    "pages"
  ],
  "properties": {
    "v": {
      "type": "integer",
      "minimum": 1
    },
    "theme": {
      "type": "string",
      "description": "Panel theme name (generated by tools/theme-import). Kept in sync with the kit THEMES registry + the app isThemeName() validator.",
      "enum": [
        "nordic-oak",
        "tech-loft",
        "pop-studio",
        "classic-study",
        "vintage-amber",
        "japandi",
        "industrial",
        "art-deco",
        "coastal",
        "cyberpunk",
        "warm-minimal",
        "gallery-white",
        "scandi-night",
        "moody-lounge",
        "terracotta",
        "emerald-study",
        "deep-ocean",
        "blush-boudoir",
        "wood-carve",
        "hifi-90",
        "hifi-steel",
        "rocker-white",
        "paddle-decora",
        "walnut-brass",
        "toggle-white",
        "neon-pill",
        "neon-white",
        "cream",
        "tech",
        "white",
        "techLed",
        "creamLed"
      ]
    },
    "pages": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/page"
      }
    }
  },
  "additionalProperties": true,
  "$defs": {
    "page": {
      "type": "object",
      "required": [
        "id",
        "type"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "title": {
          "type": "string"
        },
        "type": {
          "enum": [
            "grid",
            "clock",
            "weather",
            "alarm",
            "camera",
            "music"
          ]
        },
        "grid": {
          "type": "object",
          "required": [
            "columns",
            "rows"
          ],
          "properties": {
            "columns": {
              "type": "integer",
              "minimum": 1
            },
            "rows": {
              "type": "integer",
              "minimum": 1
            }
          },
          "additionalProperties": true
        },
        "tiles": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/tile"
          }
        },
        "alarm": {
          "$ref": "#/$defs/alarmPage"
        },
        "camera": {
          "$ref": "#/$defs/cameraPage"
        },
        "weather": {
          "type": "object",
          "description": "Weather page options. Without `entity` the page shows the first weather.* entity (mirrored on state/weather); with it, that entity (mirrored on state/<slug>). The integration adds a short daily forecast via weather.get_forecasts.",
          "properties": {
            "entity": {
              "type": "string",
              "pattern": "^weather\\."
            }
          },
          "additionalProperties": true
        }
      },
      "additionalProperties": true,
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "grid"
              }
            }
          },
          "then": {
            "required": [
              "grid",
              "tiles"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "camera"
              }
            }
          },
          "then": {
            "required": [
              "camera"
            ]
          }
        }
      ]
    },
    "alarmPage": {
      "type": "object",
      "description": "Alarm (Alarmo) keypad page config (PLAN.md §3.3, Phase 5). The panel talks to Alarmo DIRECTLY over its native MQTT topics (https://github.com/nielsfaber/alarmo#mqtt) — NOT via this integration. All fields optional; the app fills the master-alarm topic defaults. The PIN is entered on-device and published ONLY to the non-retained command topic — never stored here or on any retained topic.",
      "properties": {
        "stateTopic": {
          "type": "string",
          "minLength": 1,
          "description": "Alarmo state topic (plain-string state). Default 'alarmo/state'."
        },
        "commandTopic": {
          "type": "string",
          "minLength": 1,
          "description": "Alarmo command topic — the SINGLE master topic (default 'alarmo/command'). The panel publishes JSON {command:'arm_home'|'arm_away'|'disarm', code, area?}; the area is selected via the payload, NOT a per-area topic."
        },
        "eventTopic": {
          "type": "string",
          "minLength": 1,
          "description": "Alarmo event topic (JSON feedback: INVALID_CODE_PROVIDED, FAILED_TO_ARM, ...). Per-area = 'alarmo/<area>/event'; master default 'alarmo/event'."
        },
        "area": {
          "type": "string",
          "minLength": 1,
          "description": "Alarmo area slug to target (sent as `area` in the command payload, e.g. 'panel'). Omit for the master alarm."
        },
        "actions": {
          "type": "array",
          "description": "Which action buttons to show (default = all three). Always rendered; disabled contextually by the app.",
          "minItems": 1,
          "items": {
            "enum": [
              "disarm",
              "arm_home",
              "arm_away"
            ]
          }
        },
        "codeArmRequired": {
          "type": "boolean",
          "description": "Whether ARMING needs a PIN (gates the arm buttons; disarm always needs one). Default true. If Alarmo rejects with NO_CODE_PROVIDED the panel prompts anyway."
        },
        "screensaverWhenArmed": {
          "type": "boolean",
          "description": "When armed, show this alarm page as the screensaver instead of the clock. Default true; false opts out."
        }
      },
      "additionalProperties": true
    },
    "cameraPage": {
      "type": "object",
      "description": "Camera page: a live MJPEG view (480x270, 16:9) above a control strip with a PTZ D-pad and a camera selector. The INTEGRATION mirrors each camera entity onto its retained state/{key} topic with an absolute `stream` URL carrying HA's rotating camera access token (from entity_picture); the app renders that URL in a WebView <img>. Only the selected camera on the currently-visible page ever streams. PTZ entity ids are EXPLICIT (never derived from the camera's name) so the page works with any brand.",
      "required": [
        "cameras"
      ],
      "properties": {
        "cameras": {
          "type": "array",
          "description": "Cameras selectable on this page, in order. The first is shown on arrival. Prefer a camera's SD/substream entity: the video area is 480px wide, so an HD stream is wasted decode on the panel.",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": [
              "entity"
            ],
            "properties": {
              "entity": {
                "type": "string",
                "pattern": "^camera\\.",
                "description": "The HA camera entity to stream, e.g. 'camera.cam_cucina_esterna_sd_stream'."
              },
              "name": {
                "type": "string",
                "minLength": 1,
                "description": "Label in the camera selector. Defaults to a prettified entity id."
              },
              "ptz": {
                "type": "object",
                "description": "Pan/tilt targets. Only the directions present here get an enabled arrow; the rest render disabled. Ids are explicit because no naming convention generalises across brands (Tapo, Reolink, ONVIF all differ).",
                "properties": {
                  "up": {
                    "$ref": "#/$defs/ptzTarget"
                  },
                  "down": {
                    "$ref": "#/$defs/ptzTarget"
                  },
                  "left": {
                    "$ref": "#/$defs/ptzTarget"
                  },
                  "right": {
                    "$ref": "#/$defs/ptzTarget"
                  }
                },
                "additionalProperties": false
              }
            },
            "additionalProperties": true
          }
        },
        "baseUrl": {
          "type": "string",
          "minLength": 1,
          "description": "Override the HA base URL the stream URL is built from (the integration otherwise uses helpers.network.get_url). Escape hatch for a multi-homed/containerised HA whose auto-detected URL the panel cannot reach. Integration-only: never sent to the app."
        },
        "keepOnPage": {
          "type": "boolean",
          "description": "Suppress the stage-1 inactivity return-to-default-page while this page is showing, so you are not yanked away mid-watch. Default true. Bounded by the stage-2 screensaver, which still fires and stops the stream."
        },
        "keepAwake": {
          "type": "boolean",
          "description": "Also hold off the stage-2 screensaver while streaming. Default FALSE, deliberately: an unattended live stream on an always-on wall panel (backlight burn-in, 1.4 Mbit/s, constant JPEG decode) is exactly what the screensaver exists to prevent. The screensaver wakes on proximity, so walking up brings the camera back in about a second."
        },
        "holdRepeatMs": {
          "type": "integer",
          "minimum": 0,
          "description": "PTZ press-and-hold repeat interval in ms (0 = no repeat, single press only). Default 400."
        }
      },
      "additionalProperties": true
    },
    "ptzTarget": {
      "description": "One PTZ direction. Either a shorthand entity_id string -- the bridge derives the service from its domain (button -> button.press, switch -> switch.turn_on, script -> script.turn_on, scene -> scene.turn_on) -- or a full binding action / ordered action list, which is executed verbatim by the same executor tile bindings use. The object form is the escape hatch for cameras whose PTZ is a service call rather than an entity, e.g. {\"action\":\"call\",\"service\":\"onvif.ptz\",\"entity_id\":\"camera.front\",\"data\":{\"pan\":\"LEFT\"}}.",
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "$ref": "#/$defs/bindingList"
        }
      ]
    },
    "tile": {
      "type": "object",
      "required": [
        "id",
        "type",
        "col",
        "row"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "type": {
          "enum": [
            "button",
            "dimmer",
            "switcher",
            "split",
            "rocker"
          ]
        },
        "col": {
          "type": "integer",
          "minimum": 1
        },
        "row": {
          "type": "integer",
          "minimum": 1
        },
        "colSpan": {
          "type": "integer",
          "minimum": 1
        },
        "rowSpan": {
          "type": "integer",
          "minimum": 1
        },
        "shape": {
          "enum": [
            "square",
            "circle"
          ]
        },
        "icon": {
          "type": "string"
        },
        "iconLow": {
          "type": "string",
          "description": "dimmer only — icon at the low/dim end; the bright/high end uses `icon`."
        },
        "label": {
          "type": "string"
        },
        "statusDot": {
          "description": "false/absent = no dot; true = static dot (as before); object = dynamic dot bound to an entity (PLAN.md §3.6): the integration mirrors the entity with a generic `value` (raw HA state) and the app compares it on-device. Defaults: operator=equals, compareTo='on', color=theme status red.",
          "oneOf": [
            {
              "type": "boolean"
            },
            {
              "type": "object",
              "required": [
                "entity"
              ],
              "properties": {
                "entity": {
                  "type": "string",
                  "minLength": 1
                },
                "operator": {
                  "enum": [
                    "equals",
                    "notEqual",
                    "less",
                    "more",
                    "notLess",
                    "notMore"
                  ]
                },
                "compareTo": {
                  "oneOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                },
                "color": {
                  "type": "string"
                }
              },
              "additionalProperties": true
            }
          ]
        },
        "behavior": {
          "enum": [
            "toggle",
            "push",
            "state"
          ]
        },
        "activeMode": {
          "enum": [
            "lit",
            "inset"
          ]
        },
        "optimistic": {
          "type": "boolean",
          "description": "Optimistic UI (button/state/switcher/split): show the tapped state instantly and reconcile from the mirror. Default true; false = reflect confirmed device state only."
        },
        "entity": {
          "type": "string"
        },
        "stateKey": {
          "type": "string"
        },
        "events": {
          "type": "array",
          "items": {
            "enum": [
              "press",
              "double",
              "long",
              "release",
              "dim",
              "toggle",
              "state"
            ]
          }
        },
        "states": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/stateOption"
          }
        },
        "items": {
          "type": "array",
          "description": "switcher only — the segmented options (hero scene row). 2-6 items, unique values; direct-select: tapping a segment publishes {action:'state', value}. Per-value bindings use the 'state:<value>' key (PLAN.md §3.6). Active segment follows mirrored state/{key}.value.",
          "minItems": 2,
          "maxItems": 6,
          "items": {
            "type": "object",
            "required": [
              "value",
              "icon"
            ],
            "properties": {
              "value": {
                "type": "string",
                "minLength": 1
              },
              "icon": {
                "type": "string",
                "minLength": 1
              },
              "label": {
                "type": "string"
              }
            },
            "additionalProperties": true
          }
        },
        "buttons": {
          "type": "array",
          "description": "split only — the 2-4 sub-buttons carved into one shared raised surface (PLAN.md §3.6). Each segment toggles independently; event button id = '<tileId>.<segId>'. Segment visual state (off|on|inset) is evaluated app-side on HA mirrors; transition bindings live PER SEGMENT keyed press:off|on|inset.",
          "minItems": 2,
          "maxItems": 4,
          "items": {
            "$ref": "#/$defs/splitSegment"
          }
        },
        "orientation": {
          "enum": [
            "vertical",
            "horizontal"
          ]
        },
        "segments": {
          "type": "integer",
          "minimum": 1
        },
        "step": {
          "type": "integer",
          "minimum": 1
        },
        "holdRepeatMs": {
          "type": "integer",
          "minimum": 0
        },
        "dimmerSurface": {
          "enum": [
            "inset",
            "raised"
          ],
          "description": "dimmer only — track body style. Default 'inset' (recessed channel); 'raised' renders the outset variant that sits proud of the surface."
        },
        "bindings": {
          "$ref": "#/$defs/bindings"
        },
        "labelPosition": {
          "enum": [
            "below",
            "above"
          ],
          "description": "Where the label sits relative to the glyph: 'below' (default) = ICON over LABEL; 'above' = LABEL over ICON. On a split, each SEGMENT carries its own (the tile-level one does not apply to split segments)."
        },
        "rockers": {
          "type": "array",
          "description": "rocker only — the 1-2 on/off switches stacked vertically in this tile. Each is on/off BY DEFINITION (a light/fan/switch): it shows the entity's state and a tap toggles it. No configurable actions.",
          "minItems": 1,
          "maxItems": 2,
          "items": {
            "type": "object",
            "required": [
              "entity"
            ],
            "properties": {
              "entity": {
                "type": "string",
                "description": "The on/off entity this switch controls."
              },
              "icon": {
                "type": "string",
                "description": "Glyph in the switch's right half."
              },
              "label": {
                "type": "string",
                "description": "Text next to the icon."
              }
            },
            "additionalProperties": true
          }
        }
      },
      "additionalProperties": true,
      "allOf": [
        {
          "if": {
            "properties": {
              "behavior": {
                "const": "state"
              }
            },
            "required": [
              "behavior"
            ]
          },
          "then": {
            "required": [
              "states"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "switcher"
              }
            },
            "required": [
              "type"
            ]
          },
          "then": {
            "required": [
              "items"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "split"
              }
            },
            "required": [
              "type"
            ]
          },
          "then": {
            "required": [
              "buttons"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "rocker"
              }
            }
          },
          "then": {
            "anyOf": [
              {
                "required": [
                  "rockers"
                ]
              },
              {
                "required": [
                  "entity"
                ]
              }
            ]
          }
        }
      ]
    },
    "stateOption": {
      "type": "object",
      "required": [
        "value"
      ],
      "properties": {
        "value": {
          "type": "string",
          "minLength": 1
        },
        "icon": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "active": {
          "type": "boolean"
        }
      },
      "additionalProperties": true
    },
    "bindings": {
      "type": "object",
      "description": "Keys are event names (press|double|long|release|dim|toggle) or per-value keys for state buttons (state:<value>). Each value is a single action OR an array of actions run in order (multi-action, global upgrade PLAN.md §3.6).",
      "patternProperties": {
        "^(press|double|long|release|dim|toggle|state)$": {
          "$ref": "#/$defs/bindingList"
        },
        "^state:.+$": {
          "$ref": "#/$defs/bindingList"
        }
      },
      "additionalProperties": false
    },
    "splitSegment": {
      "type": "object",
      "description": "One sub-button of a split tile (PLAN.md §3.6). `id` is unique within the tile; the event button id is '<tileId>.<segId>'.",
      "required": [
        "id",
        "icon"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "icon": {
          "type": "string",
          "minLength": 1
        },
        "label": {
          "type": "string"
        },
        "state": {
          "$ref": "#/$defs/splitSegmentState"
        },
        "bindings": {
          "$ref": "#/$defs/splitSegmentBindings"
        },
        "labelPosition": {
          "enum": [
            "below",
            "above"
          ],
          "description": "Where the label sits relative to the glyph: 'below' (default) = ICON over LABEL; 'above' = LABEL over ICON. On a split, each SEGMENT carries its own (the tile-level one does not apply to split segments)."
        }
      },
      "additionalProperties": true
    },
    "splitSegmentState": {
      "type": "object",
      "description": "HA-driven visual state of a split segment (PLAN.md §3.6): evaluated app-side on mirrors, precedence inset > on > off, fail-safe off. `entity` is the default source for both rules; each rule may override it. The integration mirrors every referenced entity with a generic `value`.",
      "properties": {
        "entity": {
          "type": "string"
        },
        "on": {
          "$ref": "#/$defs/splitSegmentRule"
        },
        "inset": {
          "$ref": "#/$defs/splitSegmentRule"
        }
      },
      "additionalProperties": true
    },
    "splitSegmentRule": {
      "type": "object",
      "description": "A split-segment state rule — same operator grammar as a status dot. Defaults: operator=equals, compareTo='on'.",
      "properties": {
        "operator": {
          "enum": [
            "equals",
            "notEqual",
            "less",
            "more",
            "notLess",
            "notMore"
          ]
        },
        "compareTo": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ]
        },
        "entity": {
          "type": "string"
        }
      },
      "additionalProperties": true
    },
    "splitSegmentBindings": {
      "type": "object",
      "description": "Split-segment TRANSITION bindings keyed by the segment's state AT PRESS time (PLAN.md §3.6). The panel publishes {button:'<tileId>.<segId>', action:'press', state:'off|on|inset'}; the bridge routes the matching press:<state>. Each value is a single action OR an array of actions run in order.",
      "patternProperties": {
        "^press:(off|on|inset)$": {
          "$ref": "#/$defs/bindingList"
        }
      },
      "additionalProperties": false
    },
    "bindingList": {
      "description": "A single binding action OR an ordered array of actions (multi-action upgrade, PLAN.md §3.6). Arrays are executed in order by the bridge; min 1 entry.",
      "oneOf": [
        {
          "$ref": "#/$defs/binding"
        },
        {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/binding"
          }
        }
      ]
    },
    "binding": {
      "type": "object",
      "required": [
        "action"
      ],
      "properties": {
        "action": {
          "enum": [
            "toggle",
            "turn_on",
            "turn_off",
            "brightness",
            "call",
            "scene",
            "script",
            "none"
          ]
        },
        "service": {
          "type": "string",
          "pattern": "^[a-z_]+\\.[a-z0-9_]+$"
        },
        "entity_id": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ]
        },
        "data": {
          "type": "object"
        }
      },
      "additionalProperties": true,
      "allOf": [
        {
          "if": {
            "properties": {
              "action": {
                "const": "call"
              }
            },
            "required": [
              "action"
            ]
          },
          "then": {
            "required": [
              "service"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "action": {
                "const": "scene"
              }
            },
            "required": [
              "action"
            ]
          },
          "then": {
            "required": [
              "entity_id"
            ]
          }
        }
      ]
    }
  }
}
